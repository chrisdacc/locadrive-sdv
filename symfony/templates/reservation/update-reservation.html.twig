{% extends 'base.html.twig' %}

{% block main %}

    <h1>Mettre à jour une facture</h1>

    <form method="POST">

        <label> Vehicle
            <input type="text" name="title" value="{{ reservation.vehicle }}">
        </label>

        <label> Start Date
            <input type="text" name="description" value="{{ reservation.startDate }}">
        </label>

        <label> End Date
            <input type="text" name="description" value="{{ reservation.endDate }}">
        </label>

        <label> Client
            <input type="text" name="description" value="{{ reservation.user }}">
        </label>

        <label> Price
            <input type="number" step="0.01" name="price" value="{{ reservation.totalPrice }}">
        </label>

        <button type="submit">Modifier la reservation</button>

    </form>

{% endblock %}

